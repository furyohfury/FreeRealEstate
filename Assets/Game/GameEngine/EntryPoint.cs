using Debugging;
using UnityEngine;
using SimulationSystemGroup = Unity.Entities.SimulationSystemGroup;
using World = Unity.Entities.World;

namespace GameEngine
{
    public sealed class EntryPoint : MonoBehaviour
    {
//         private void Start()
//         {
//             var world = World.DefaultGameObjectInjectionWorld;
//             var mySystemHandle = world.CreateSystem(typeof(SpawnSystem));
//
// // Если хочешь засунуть её в конкретную группу вручную:
//             var simulationGroup = world.GetExistingSystemManaged<SimulationSystemGroup>();
//             simulationGroup.AddSystemToUpdateList(mySystemHandle);
//         }
        
    }
}
